## 興味ある事
- rust
- bevy
  - `bevy::asset`関係の機能の利用
  - One-Shot Systemsの利用
  - [x] Query\<T>のバリエーションの利用（Singl\<T>）
  - ＜v0.18リリース後＞
    - [ ] `bevy_camera_controller`の利用
    - [ ] `cargo feature collections`の利用
- tigtag
  - 全画面表示の改良
    - [x] UIの不具合修正（中央に寄らず左上に寄ってしまう）
      - [x] UI位置調整処理の適正化
    - [x] スケールファクターをモニタの解像度に連動させる（ハードコードしない）
      - [ ] ＜不具合？＞プログラム実行中にモニターを縦横回転したらフリーズした
    - [x] ウィンドウ表示中のモニターで全画面表示する（マルチモニター対応）
    - [ ] スケールファクターを操作するロジックを変更する
      - スケールファクターの変化を検出するSystemを用意して結果をResouceへ登録する
      - 他のSystemはResourceを参照する
  - `Window.fit_canvas_to_parent`に対応
    - スケールファクターをブラウザの描画領域(Canvas？)と連動させる
    - UIの位置ずれを調整する
  - 入力処理の改良（プレイヤーキャラを気持ち良く操作できるように）
  - demoのアルゴリズムの改良
    - [ ] より高得点を出せるようにする
    - [ ] clippy警告対策（demo_play\auto_drive.rs:178）  
    178 - for i in 2..path_0_len  
    178 + for (i, \<item>) in paths.iter().enumerate().take(path_0_len).skip(2)
  - proc-macroのソースコードの改修
  - コンフィグ機能の実装
    - [ ] 入力のバインド（キーボードとゲームパッド）
    - [ ] 速度調整
  - データセーブ機能の実装
    - [ ] ハイスコア
    - [ ] コンフィグ（コンフィグ実装後）
